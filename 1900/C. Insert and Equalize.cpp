#include <bits/stdc++.h>
using namespace std;
#define ll long long

void sr_sol(ll n, ll a[], ll count)
{
    int mx=max_element(a, a+n)-a;
    
   
}

int main()
{
    ll t;
    cin >>t;
    while(t--)
    {
       ll n;
       cin >>n;
       ll a[n];
         for(ll i=0; i<n; i++)
         {
              cin >>a[i];
         }

        ll count=0;

      sr_sol(n, a, count);

    }
    return 0;
}
